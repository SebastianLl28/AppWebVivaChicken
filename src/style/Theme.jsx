import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#a55e42", // Cambia este valor al color primario deseado
      //main: "#e5133a", // Cambia este valor al color primario deseado
    },
  },
});

export default theme;
